---
sentry:
  hosts:
    sentry.nos.social:
      domains: 
        - sentry.nos.social
      cloudflare_api_token: '{{ vault_cloudflare_api_token }}' 
      cert_email: ops@nos.social
      homedir: /home/{{ admin_username }}
 
  